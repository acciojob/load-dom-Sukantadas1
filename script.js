  document.addEventListener('DOMContentLoaded', function () {
      // DOM is loaded, write the string
      const body = document.querySelector('body');
      body.textContent = 'DOM load success';
    });
